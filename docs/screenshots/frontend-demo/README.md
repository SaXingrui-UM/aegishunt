# Frontend demo validation

Browser-captured evidence for the Phase 14 corrective pull request:

- `before-overview.png`: baseline duplicate Streamlit navigation and developer UI.
- `after-overview.png`: streamlined end-to-end overview.
- `after-model-lab.png`: effective runtime models and fusion policy.
- `after-evaluation.png`: verified known-group and held-out-family evidence.
- `after-evaluation-loao.png`: verified five-family LOAO aggregate and zero-Recall evidence.

Screenshots are captured from the real base-Compose deployment in the in-app
Browser's 1280×720 desktop viewport. Separate responsive checks cover the
required desktop widths without using these images as substitutes for runtime
verification.
