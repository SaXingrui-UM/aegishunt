application:
  environment: development

database:
  url: sqlite:///data/aegishunt.db
  echo: false
  busy_timeout_ms: 5000
  enable_wal: true
  enable_foreign_keys: true

ingestion:
  storage_root: data/raw
  sample_root: data/sample
  max_upload_bytes: 52428800
  chunk_size_bytes: 65536
  max_records: 100000

flows:
  idle_timeout_seconds: 60.0
  active_timeout_seconds: 300.0
  max_packets_per_flow: 10000
  max_active_flows: 100000
  max_packet_bytes: 262144

datasets:
  registry_path: configs/datasets/registry.yaml
  label_mapping_root: configs/label_mappings
  raw_root: data/raw/datasets
  interim_root: data/interim/datasets
  processed_root: data/processed/datasets
  reports_root: reports/datasets
  max_download_bytes: 10737418240
  max_archive_members: 10000
  max_extracted_bytes: 21474836480
  near_duplicate_tolerance: 0.000001
  demo_seed: 4204
  train_ratio: 0.6
  validation_ratio: 0.2
  test_ratio: 0.2

supervised:
  training_config_path: configs/models/supervised.yaml
  artifact_root: artifacts/models/supervised
  reports_root: reports/models/supervised
